{{#each hoverPhotos as | hoverPhoto i|}}
	{{#ifEquals introFragmentType "hoverable"}}
		<img class="intro__image 
					js--project--{{ introFragmentKey }}" 
			src=".{{ introFragmentImagePath }}">
	{{/ifEquals}}
{{/each}}
